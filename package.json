{
  "name": "avaco-runsite-relay",
  "version": "1.0.0",
  "private": true,
  "description": "A lightweight Node.js HTTP relay for RunSite.",
  "main": "server.js",
  "scripts": {
    "build": "npm run check",
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
